/**
 * CrackIt — Seed Data Generator (run once in browser console or via script tag)
 * Generates large mock datasets and saves to assets/data/ via download
 * Primary data loading uses storage.js generators + JSON fetch
 */

// This file documents the data schema. Actual seed data is generated by:
// 1. assets/data/*.json files (when served via HTTP)
// 2. CrackItStorage.generators in storage.js (fallback)

const CrackItDataSchema = {
  projects: {
    fields: ['id', 'name', 'description', 'status', 'priority', 'progress', 'tags', 'color', 'pinned', 'members', 'createdAt', 'updatedAt'],
    count: 50
  },
  notes: { fields: ['id', 'title', 'content', 'folder', 'tags', 'pinned', 'favorite', 'createdAt', 'updatedAt', 'wordCount'], count: 50 },
  files: { fields: ['id', 'name', 'folder', 'type', 'size', 'favorite', 'recent', 'createdAt', 'modifiedAt', 'path'], count: 120 },
  reports: { fields: ['id', 'title', 'summary', 'severity', 'status', 'findings', 'riskScore', 'author', 'createdAt', 'updatedAt', 'tags'], count: 30 },
  tasks: { fields: ['id', 'title', 'status', 'priority', 'project', 'assignee', 'dueDate', 'createdAt'], count: 120 },
  notifications: { fields: ['id', 'title', 'text', 'type', 'read', 'category', 'createdAt'], count: 10 },
  commands: { fields: ['id', 'label', 'icon', 'action', 'target', 'shortcut'], count: 15 }
};
